name: CI-CD Demo

on:
  push:
    branches:
      - master
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Run CI test
        run: echo "CI/CD pipeline is running successfully"
